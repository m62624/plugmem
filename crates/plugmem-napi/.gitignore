# Generated by @napi-rs/cli at build time (the platform addon, the loader and
# the type surface) — reproduced by CI, never committed.
*.node
index.js
index.d.ts

# npm tooling / packaging output
node_modules/
npm/
