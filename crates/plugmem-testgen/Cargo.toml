[package]
name = "plugmem-testgen"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Deterministic synthetic corpus generator for plugmem tests and benchmarks. Internal."
# Internal tooling, never published.
publish = false

[dependencies]
# The PRNG is implemented in place (specs/11 §3): reproducibility of the
# corpora must not depend on any external crate's stream stability.
plugmem-core = { workspace = true, features = ["std"] }

[lints]
workspace = true
